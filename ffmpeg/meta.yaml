package:
    name: ffmpeg
    version: 2.5.3

source:
    fn: ffmpeg-2.5.3.tar.bz2
    url: http://ffmpeg.org/releases/ffmpeg-2.5.3.tar.bz2

build:
    number: 0

requirements:
    build:
        - x264 # [unix]
        - openssl
        - yasm # [unix]
        - mingw # [win]
    run:
        - x264 # [unix]
        - openssl

about:
    home: http://www.ffmpeg.org/
    license: GPL-2
